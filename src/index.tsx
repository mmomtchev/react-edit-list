import * as React from 'react';
import ReactEditList from './EditList';

export {Schema, Props, getKey} from './base';
export {default as ReactEditList} from './EditList';

export default ReactEditList;
